fetch-retries=10
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
# Disable undici if possible via setting, though pnpm usually uses its own logic
# This often helps with "Value of 'this' must be of type URLSearchParams"
network-concurrency=1
